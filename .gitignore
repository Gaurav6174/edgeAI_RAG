# Python
.venv/
venv/
env/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Environment files
.env
*.env
# backend/.env

# Node / Vite
node_modules/
build/
coverage/
frontend/node_modules/
frontend/build/
frontend/.vite/
# frontend/dist/ is intentionally tracked - the board has no reliable Node.js/Vite
# build environment, so the backend serves this pre-built, committed bundle
# directly (see backend/main.py, study.md). Rebuild + commit after frontend changes.

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Personal / exported chat logs (contains usernames, IPs - keep out of the repo)
Claude-Nvidia Jetson project ideas for internship.txt

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Generated data
data/index/*
!data/index/.gitkeep
data/uploads/*
!data/uploads/.gitkeep
data/eval/*
!data/eval/.gitkeep
